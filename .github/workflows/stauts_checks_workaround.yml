name: Python package CI/CD

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'docs/*'

jobs:
  test:

    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.8", "3.9", "3.10"]

    steps:
    - run: echo "checks"
   




                         
                         
                         
