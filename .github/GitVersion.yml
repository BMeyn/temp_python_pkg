mode: Mainline
branches:
  main:
    regex: main
  feature:
    regex: ^features?[/-]
    increment: minor
    source-branches: ['main']
  bugfix:
    regex: ^bugfix?[/-]
    increment: patch
    source-branches: ['main']
